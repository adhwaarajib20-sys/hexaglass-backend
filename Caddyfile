:80 {
    encode gzip

    root * /app/public
    php_fastcgi localhost:9000
    try_files {path} /index.php

    @notFile {
        not file
        not directory
    }
    rewrite @notFile /index.php
}
